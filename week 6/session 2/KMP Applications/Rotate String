class Solution {
    public boolean rotateString(String s, String goal) {
        // Quick check: lengths must match
        if (s.length() != goal.length()) return false;
        // Concatenate s with itself
        String doubled = s + s;
        // Check if goal is inside doubled
        return doubled.contains(goal);
    }
}
